import { LoginForm } from "@/features/auth/views/LoginForm";
import React from "react";

const loiginPages = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default loiginPages;
